process HLT = {

      untracked PSet configurationMetadata = {
      untracked string version = "$Revision: 1.5 $"
      untracked string name = "$Source: /cvs_server/repositories/CMSSW/CMSSW/HLTrigger/Configuration/test/RelVal_HLTFromRaw_10pbConditions.cfg,v $"
      untracked string annotation = "Standard CFG to do raw2digi + HLT; 10pb Conditions for Mis Calib/Alignment ."
    }

// Message Logger
include "FWCore/MessageLogger/data/MessageLogger.cfi"

// Source of events
untracked PSet maxEvents = {untracked int32 input = 100}

source = PoolSource {
        untracked vstring fileNames = {
        "file:RelVal_Digi_Raw.root"
  }
}

// Conditions data
// Digi to raw: Conditions should be included through StandardSequences
include "Configuration/StandardSequences/data/Frontier10pbConditions.cff"
include "HLTrigger/Configuration/data/rawToDigi/Frontier10pbConditions.cff"
// include "Configuration/StandardSequences/data/Frontier100pbConditions.cff"
// include "HLTrigger/Configuration/data/rawToDigi/Frontier100pbConditions.cff"

// HLT table to run (set of [HLT] paths)
include "HLTrigger/Configuration/data/main/HLTtable.cff"
include "L1TriggerConfig/L1GtConfigProducers/data/L1GtFactorsUnprescale.cff"
include "HLTrigger/Configuration/data/common/HLTPrescaleReset.cff"

include "Configuration/EventContent/data/EventContent.cff"
module hltPoolOutput = PoolOutputModule {
	using FEVTSIMEventContent
	untracked string fileName = "file:HLTFromDigiRaw.root"
	untracked int32 basketSize = 4096
	untracked PSet dataset ={
            untracked string dataTier = "GEN-SIM-DIGI-RECO"
        }
}
//Keep  GEN-SIM, MC digis, L1, HLT, RAW
//Drop digis from raw2digi unpacking within HLT
replace hltPoolOutput.outputCommands += "drop *_*Digis_*_HLT"

endpath HLTPoolOutput = {hltPoolOutput}

}
